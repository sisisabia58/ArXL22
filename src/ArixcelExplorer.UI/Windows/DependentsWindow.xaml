<Window x:Class="ArixcelExplorer.UI.Windows.DependentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Arixcel Explorer"
        ToolTip="↑/↓ navigate · Enter keep · Esc back"
        Height="480"
        Width="480"
        MinHeight="320"
        MinWidth="360"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True"
        Topmost="False"
        PreviewKeyDown="Window_PreviewKeyDown"
        Closing="Window_Closing"
        Loaded="Window_Loaded">
  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/ArixcelExplorer.UI;component/Themes/ArixcelTheme.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Window.Resources>
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" BorderBrush="{StaticResource ArixcelBorderBrush}" BorderThickness="1" CornerRadius="2">
      <DataGrid x:Name="DependentsGrid"
                ItemsSource="{Binding Rows}"
                SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"
                AutoGenerateColumns="False"
                HeadersVisibility="Column"
                GridLinesVisibility="Horizontal"
                IsReadOnly="True"
                SelectionMode="Single"
                SelectionUnit="FullRow"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CanUserResizeRows="False"
                CanUserReorderColumns="False"
                RowHeaderWidth="0"
                BorderThickness="0"
                Background="White">
        <DataGrid.Resources>
          <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#0078D7" />
          <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFFFFF" />
          <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#0078D7" />
          <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#FFFFFF" />
        </DataGrid.Resources>
        <DataGrid.RowStyle>
          <Style TargetType="DataGridRow" BasedOn="{StaticResource ArixcelExplorerRowStyle}" />
        </DataGrid.RowStyle>
        <DataGrid.CellStyle>
          <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Style.Triggers>
              <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource ArixcelHighlightBrush}" />
                <Setter Property="Foreground" Value="{StaticResource ArixcelHighlightTextBrush}" />
              </Trigger>
            </Style.Triggers>
          </Style>
        </DataGrid.CellStyle>
        <DataGrid.Columns>
          <DataGridTextColumn Header="Element" Binding="{Binding AddressDisplay}" Width="*" />
          <DataGridTextColumn Header="Count" Binding="{Binding CountDisplay}" Width="60" />
          <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="90" />
          <DataGridTextColumn Header="Location" Binding="{Binding Address}" Width="130" />
        </DataGrid.Columns>
      </DataGrid>
    </Border>

    <DockPanel Grid.Row="1" Margin="0,8,0,0" LastChildFill="False">
      <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" Foreground="#555" />
      <Button Content="OK" Width="72" Height="24" DockPanel.Dock="Right"
              Click="Ok_Click" />
    </DockPanel>
  </Grid>
</Window>
