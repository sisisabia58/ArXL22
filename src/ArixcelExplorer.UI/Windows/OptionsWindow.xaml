<Window x:Class="ArixcelExplorer.UI.Windows.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EXLerate Options"
        Height="520"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Text="Formula Explorer Options"
               FontSize="15"
               FontWeight="SemiBold"
               Margin="0,0,0,12" />

    <StackPanel Grid.Row="1">
      <TextBlock Text="Close behavior" FontWeight="SemiBold" Margin="0,0,0,4" />
      <ComboBox x:Name="CloseBehaviorCombo" Margin="0,0,0,12">
        <ComboBoxItem Content="Enter keeps selection, Esc navigates back" Tag="EnterKeepsSelection" />
        <ComboBoxItem Content="Esc closes and navigates back automatically" Tag="EscNavigatesBack" />
      </ComboBox>

      <TextBlock Text="Origin highlight (hex)" FontWeight="SemiBold" Margin="0,0,0,4" />
      <TextBox x:Name="OriginHighlightBox" Margin="0,0,0,12" />

      <TextBlock Text="Precedent highlight (hex)" FontWeight="SemiBold" Margin="0,0,0,4" />
      <TextBox x:Name="PrecedentHighlightBox" Margin="0,0,0,12" />

      <TextBlock Text="Max trace depth" FontWeight="SemiBold" Margin="0,0,0,4" />
      <TextBox x:Name="MaxDepthBox" Margin="0,0,0,12" />

      <TextBlock Text="Dependents warning threshold" FontWeight="SemiBold" Margin="0,0,0,4" />
      <TextBox x:Name="DependentsWarningBox" Margin="0,0,0,12" />

      <CheckBox x:Name="ConfirmLargeScanBox"
                Content="Confirm before scanning large dependent sets"
                Margin="0,0,0,12" />
    </StackPanel>

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Save" Width="80" Margin="0,0,8,0" Click="Save_Click" IsDefault="True" />
      <Button Content="Cancel" Width="80" Click="Cancel_Click" IsCancel="True" />
    </StackPanel>
  </Grid>
</Window>
